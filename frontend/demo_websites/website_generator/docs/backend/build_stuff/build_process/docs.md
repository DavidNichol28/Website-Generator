@@work_title="Docs Backend Build Stuff Build Process Docs"
# Build Process Docs
## Purpose
Handles turning the directory into a config on a file-by-file level. 

## Basic Example
  pub fn build_handler(target_location: &Path, output_dir: &Path, build_command: &str) {
    ...
  }
